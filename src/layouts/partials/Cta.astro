---
import config from "@config/config.json";

const {
  call_to_action: {
    title,
    description,
    button: { label, link },
  },
} = config;
---

<section class="px-5 py-20 xl:py-[120px]">
  <div class="container">
    <div
      class="bg-gradient row justify-center rounded-b-[80px] rounded-t-[20px] px-[30px] pb-[75px] pt-16"
    >
      <div class="lg:col-11">
        <div class="row">
          <div class="lg:col-7">
            <h4 class="h2 text-white">Ready to Grow ! <br/>Join us.</h4>
            <a class="btn btn-white mt-8" href={link}>Contact us</a>
          </div>
          <div class="mt-8 lg:col-5 lg:mt-0">
            <p class="text-white">
              Join us today and experience the difference firsthand. Let's embark on a journey to success together!
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
